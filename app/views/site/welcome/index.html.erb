<h1>site::welcome#index</h1>
<p>find me in app/views/welcome/index.html.erb</p>

<% if user_signed_in? %>
  <p>Usuário está logado | <%= current_user.email %></p>
<% else %>
  <p>Não está logado</p>
<% end %>

<% if admin_signed_in? %>
  <p>Usuário está logado | <%= current_admin %></p>
<% else %>
  <p>Não está logado</p>
<% end %>